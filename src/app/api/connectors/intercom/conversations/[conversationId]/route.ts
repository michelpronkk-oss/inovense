import { NextRequest, NextResponse } from "next/server";
import { getIntercomConversationDetail, getStoredIntercomCredential, resolveIntercomAccessToken } from "@/lib/connectors/intercom";
import { createSupabaseAdmin, hasSupabaseAdminConfig } from "@/lib/server/supabase-admin";
import { resolveWorkspaceContext } from "@/lib/os/workspace";
export async function GET(req: NextRequest, ctx: { params: Promise<{ conversationId: string }> }) { if (!hasSupabaseAdminConfig()) return NextResponse.json({ error: "Supabase is not configured." }, { status: 503 }); const { conversationId } = await ctx.params; const supabase = createSupabaseAdmin(); const context = await resolveWorkspaceContext({ workspaceId: req.nextUrl.searchParams.get("workspaceId") || undefined, supabase }); if (!context.ok) return NextResponse.json({ error: context.error }, { status: context.status }); const credential = await getStoredIntercomCredential(context.workspaceId, supabase); if (!credential) return NextResponse.json({ error: "Intercom is not connected." }, { status: 404 }); try { const region = String(credential.metadata?.region) as "us" | "eu" | "au"; const token = await resolveIntercomAccessToken({ workspaceId: context.workspaceId, credential, supabase }); return NextResponse.json({ conversation: await getIntercomConversationDetail(token, region, conversationId) }); } catch (error) { return NextResponse.json({ error: error instanceof Error ? error.message : "Could not load Intercom conversation." }, { status: 502 }); } }

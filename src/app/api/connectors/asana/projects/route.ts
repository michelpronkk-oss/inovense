import { NextRequest, NextResponse } from "next/server";
import { getStoredAsanaCredential, listAsanaProjects, resolveAsanaAccessToken } from "@/lib/connectors/asana";
import { createSupabaseAdmin, hasSupabaseAdminConfig } from "@/lib/server/supabase-admin";
import { resolveWorkspaceContext } from "@/lib/os/workspace";
export async function GET(req: NextRequest) { if (!hasSupabaseAdminConfig()) return NextResponse.json({ error: "Supabase is not configured." }, { status: 503 }); const workspaceGid = req.nextUrl.searchParams.get("asanaWorkspaceId")?.trim(); if (!workspaceGid) return NextResponse.json({ error: "asanaWorkspaceId is required." }, { status: 400 }); const supabase = createSupabaseAdmin(); const context = await resolveWorkspaceContext({ workspaceId: req.nextUrl.searchParams.get("workspaceId") || undefined, supabase }); if (!context.ok) return NextResponse.json({ error: context.error }, { status: context.status }); try { const credential = await getStoredAsanaCredential(context.workspaceId, supabase); if (!credential) return NextResponse.json({ error: "Asana is not connected." }, { status: 404 }); const token = await resolveAsanaAccessToken({ workspaceId: context.workspaceId, credential, supabase }); return NextResponse.json({ projects: await listAsanaProjects(token, workspaceGid) }); } catch { return NextResponse.json({ error: "Could not load Asana projects." }, { status: 502 }); } }
